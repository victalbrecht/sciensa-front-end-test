import { PreCurrencyPipe } from './pre-currency.pipe';

describe('PreCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new PreCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
